# ---- PREFIX -------------------------------------------------

# Change prefix from Ctrl+b → Ctrl+a
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# ---- GENERAL -----------------------------------------------

# Enable mouse support
set -g mouse on

# Faster key repeat
set -s escape-time 0

# Better scrolling
set -g history-limit 100000

# ---- SPLITS ------------------------------------------------

# Remove weird default splits
unbind '"'
unbind %

# Use = and - for splits
bind = split-window -h
bind - split-window -v

# ---- PANE NAVIGATION ---------------------------------------

# Switch panes with Ctrl+a + arrow keys
bind Up    select-pane -U
bind Down  select-pane -D
bind Left  select-pane -L
bind Right select-pane -R

# Resize panes with Ctrl+a + HJKL
bind -r H resize-pane -L 5
bind -r L resize-pane -R 5
bind -r K resize-pane -U 5
bind -r J resize-pane -D 5

# ---- WINDOWS -----------------------------------------------

# Easier window switching
bind -n M-1 select-window -t 1
bind -n M-2 select-window -t 2
bind -n M-3 select-window -t 3
bind -n M-4 select-window -t 4
bind -n M-5 select-window -t 5

# ---- PANE MANAGEMENT ---------------------------------------

# Kill pane / window
bind x kill-pane
bind w kill-window

# Toggle pane zoom
bind z resize-pane -Z

# ---- WINDOW NAVIGATION -------------------------------------
# Switch windows with Alt + arrows (no prefix)
bind -n M-Up   select-window -p
bind -n M-Down select-window -n

# ---- RELOAD CONFIG -----------------------------------------

bind r source-file ~/.tmux.conf \; display-message "tmux config reloaded"

# ---- PLUGINS (tpm) -----------------------------------------

set-environment -g TMUX_PLUGIN_MANAGER_PATH '~/.tmux/plugins'

# Plugin manager
set -g @plugin 'tmux-plugins/tpm'

# Catppuccin theme
set -g @plugin 'catppuccin/tmux'

# ---- CATPPUCCIN CONFIG -------------------------------------

# Theme flavor
set -g @catppuccin_flavor 'mocha'

# Window styling
set -g @catppuccin_window_status_style "rounded"

# Status bar
set -g @catppuccin_status_background "default"

# Directory and application modules
set -g @catppuccin_directory_text "#{b:pane_current_path}"

# Pane borders: bright active, dim inactive
set -g @catppuccin_pane_status_enabled "no"
set -g @catppuccin_pane_active_border_style "fg=#b4befe"
set -g @catppuccin_pane_border_style "fg=#45475a"

# ---- TPM INIT (must be last) ------------------------------
run '~/.tmux/plugins/tmux/catppuccin.tmux'
run '~/.tmux/plugins/tpm/tpm'

# These must come AFTER catppuccin loads (no E: prefix for dynamic vars)
set -gF status-right "#{@catppuccin_status_directory}#{@catppuccin_status_session}"
set -g status-left ""

# Pane border: show repo (branch) for git dirs, directory name otherwise
# Active pane gets colored label, inactive is dim
set -g pane-border-status bottom
set -g pane-border-format "#{?pane_active,#[fg=#b4befe bold],#[fg=#6c7086]} #(basename `git -C #{pane_current_path} rev-parse --show-toplevel 2>/dev/null || echo #{pane_current_path}`)#{?#{!=:#(git -C #{pane_current_path} branch --show-current 2>/dev/null),}, (#(git -C #{pane_current_path} branch --show-current 2>/dev/null)),}#{?#{!=:#{pane_current_command},zsh}, · #{pane_current_command},} #[default]"
set -g status-interval 5
